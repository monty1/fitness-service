spring:
  application:
    name: activityservice
  datasource:
    url: jdbc:postgresql://localhost:5432/fitness-activity
    username: postgres
    password: root
  jpa:
    hibernate:
      ddl-auto: update
    database-platform: org.hibernate.dialect.PostgreSQLDialect

server:
  port: 8081

eureka:
  client:
    service-url:
      # Point to the Eureka Server instance
      defaultZone: http://localhost:8761/eureka


